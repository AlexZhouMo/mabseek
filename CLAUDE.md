# MabSeek 项目规则

本文件是 MabSeek 项目的强制规则，对本仓库内的所有 AI 协作会话生效。

## 【强制】提交到 GitHub 前，必须确认「一键部署脚本」能让本次改动在阿里云生产环境正常持续集成

MabSeek 生产环境部署在阿里云，通过 `deploy/deploy-mabseek.sh` 一键部署/更新，支持在线（GitHub git 拉取）与离线（`deploy/pack-mac.sh` 打的 tar 包）两种模式。两种模式都**保留生产数据库 `data/` 与上传目录**，且 `data/*.sqlite` 被 `.gitignore` 排除、**不随代码同步**。

因此，仅改代码或 seed 不足以让数据库相关的改动在生产生效——`seed.php` 是「有数据即整表跳过」，生产库里的历史记录不会被更新，会导致线上死链或旧值。

**触发条件：** 改动涉及代码、静态资源、数据库内容、部署脚本、seed 或迁移脚本时，执行本检查。**纯文档 / 纯注释、不影响部署链路的改动可跳过**（在提交信息里注明「无部署影响」）。

**逐项核对：**

1. **数据库变动同步** — 若改动涉及数据库里的内容（`snippets` 文案、各表记录等），**必须同时**做到：
   - 更新 `bin/seed.php` 里的种子默认值（保证全新部署正确）；
   - 编写幂等迁移脚本 `bin/migrate-*.php`（就地订正历史库存量记录）；
   - 在 `deploy/deploy-mabseek.sh` 的 seed 步骤之后接入该迁移脚本调用。
2. **静态资源完整** — 若新增 / 改名图片、CSS、JS：确认文件已 `git add`、代码引用路径已更新、且未被 `.gitignore` 排除。
3. **迁移幂等安全** — 新迁移脚本必须幂等（可反复运行，无旧值时 0 改动），且不误伤用户数据（`public/assets/images/uploads/` 上传图、管理员在后台改过的文案）。
4. **双部署模式覆盖** — 确认在线（git 拉取）与离线（`pack-mac.sh` 打包的 tar）两种模式都能拿到本次变动，尤其 `deploy/pack-mac.sh` 的打包范围覆盖新增文件。

**验证方式（实测）：** 涉及数据库 / 迁移脚本的改动，须用临时库模拟生产「旧状态」实测——跑迁移脚本、验证幂等（二次运行 0 改动）、验证页面渲染正确；并对改动的 PHP / bash 文件跑 `php -l` / `bash -n` 语法检查。仅纯文档 / 纯注释改动可省略实测。
